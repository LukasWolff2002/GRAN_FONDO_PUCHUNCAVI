<%= stylesheet_link_tag 'configuracion_base' %>
<%= stylesheet_link_tag 'jumbotron' %>
<%= stylesheet_link_tag 'home/portada' %>
<%= stylesheet_link_tag 'home/countdown' %>
<%= stylesheet_link_tag 'home/carrusel' %>
<%= stylesheet_link_tag 'circulos' %>
<%= stylesheet_link_tag 'home/auspiciadores' %>


<div class="jumbotron text-white" 
    style="background: linear-gradient(to right, var(--primary-color), rgba(10, 46, 61, 0.65), rgba(10, 46, 61, 0)),
            linear-gradient(to bottom, var(--white-color), rgba(0, 0, 0, 0.01), rgba(10, 46, 61, 0)),
            url('<%= asset_path('home/portada.JPG') %>'); 
            background-size: cover; 
            background-position: center 40%; /* Ajusta la posición vertical de la imagen */ 
            background-repeat: no-repeat; 
            position: relative;">
  
  <!-- Logo superpuesto -->
  <div class="logo-container">
    <img src="<%= asset_path('logo.png') %>" alt="Logo" class="logo">
  </div>

  <!-- Contenido del jumbotron -->
  <div class="content-container">
    <!-- Texto del evento -->
    <div class="col-md-6 px-0 text-start">
      <h1 class="font-din-condensed-small-caps title" style="color: var(--white-color)">gran fondo puchuncaví</h1>
      <p class="font-din-condensed subtitle" style="color: var(--tertiary-color)">Un evento que desafiara tus limites</p>
      <!-- Botón de inscripción -->
      <a href="#inscribirse" class="btn btn-register" style="background-color: var(--white-color)">Inscribirse</a>
    </div>
  </div>
</div>

<div class='container' style='margin-top: 2rem;'></div>

<div id="countdown" class="countdown-container">
  <div class="countdown-item">
    <span class="countdown-number" id="days">00</span>
    <span class="countdown-label">días</span>
  </div>
  <div class="countdown-item">
    <span class="countdown-number" id="hours">00</span>
    <span class="countdown-label">horas</span>
  </div>
  <div class="countdown-item">
    <span class="countdown-number" id="minutes">00</span>
    <span class="countdown-label">minutos</span>
  </div>
  <div class="countdown-item">
    <span class="countdown-number" id="seconds">00</span>
    <span class="countdown-label">segundos</span>
  </div>
</div>



<div class="container marketing">

  <!-- Row for circles -->
  <div class="row d-flex justify-content-center text-center">
    <div class="d-flex justify-content-center">
      <!-- Circle with background image -->
      <div class="circle-container">
        <div class="circle-background" style="background-image: url('<%= asset_path('home/kilometraje_1.png') %>');"></div>
        <img class="rounded-circle" width="200" height="200">
      </div>

      <!-- Circle with background image -->
      <div class="circle-container">
        <div class="circle-background" style="background-image: url('<%= asset_path('home/kilometraje_1.png') %>');"></div>
        <img class="rounded-circle" width="140" height="140">
      </div>
    </div>
  </div>

  <!-- Single text centered below -->
  <div class="row text-center mt-4">
    <div class="col-12">
      <p class="font-din-condensed description">La distancia principal consta de x kilometros con x metros de ascenso. Un desa</p>
    </div>
  </div>
</div>

<div class="sponsors-container">
  <!-- Título de auspiciadores -->
  <div class="sponsors-title">
    <h2 class="font-din-condensed-small-caps" style="margin: 0;">auspiciadores</h2>
  </div>

  <!-- Contenedor de logos -->
  <div class="sponsors-wrapper">
    <% 10.times do %>
      <div class="sponsor-item">
        <img src="<%= asset_path('logo.png') %>" alt="Auspiciador" class="sponsor-logo">
      </div>
    <% end %>
  </div>
</div>

<div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel" data-bs-interval="3000">
  <div class="carousel-inner">
    <!-- Imagen 1 -->
    <div class="carousel-item active">
      <img src="<%= asset_path('home/portada.JPG') %>" class="d-block w-100" alt="Imagen 1">
    </div>
    <!-- Imagen 2 -->
    <div class="carousel-item">
      <img src="<%= asset_path('nuestra_historia/portada.JPG') %>" class="d-block w-100" alt="Imagen 2">
    </div>
    <!-- Imagen 3 -->
  
  </div>
</div>








<script>
  //Reloj
  document.addEventListener("DOMContentLoaded", function () {
    // Fecha objetivo (modifica según tu necesidad)
    const targetDate = new Date("2024-12-31T23:59:59").getTime();

    // Actualización cada segundo
    const countdownInterval = setInterval(function () {
      const now = new Date().getTime();
      const distance = targetDate - now;

      // Cálculo de días, horas, minutos y segundos
      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);

      // Actualización del HTML
      document.getElementById("days").textContent = days.toString().padStart(2, "0");
      document.getElementById("hours").textContent = hours.toString().padStart(2, "0");
      document.getElementById("minutes").textContent = minutes.toString().padStart(2, "0");
      document.getElementById("seconds").textContent = seconds.toString().padStart(2, "0");

      // Cuando llega a la fecha objetivo
      if (distance < 0) {
        clearInterval(countdownInterval);
        document.getElementById("countdown").innerHTML = "¡El evento ha comenzado!";
      }
    }, 1000);
  });

  // Carrusel
  document.addEventListener("DOMContentLoaded", () => {
  const wrapper = document.querySelector(".carousel-wrapper");
  const slides = document.querySelectorAll(".carousel-slide");
  const totalSlides = slides.length;
  let currentSlide = 0;

  setInterval(() => {
    currentSlide = (currentSlide + 1) % totalSlides; // Ciclo entre las imágenes
    wrapper.style.transform = `translateX(-${currentSlide * 100}%)`;
  }, 1500); // Cambia cada 3 segundos
});

</script>








